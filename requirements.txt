# Jarvis Voice Assistant - runtime dependencies
# Install into the same Python used by launch_jarvis.bat / desktop shortcut.

# Core LLM (cloud fallback via OpenRouter)
openai>=1.0.0
requests

# Local LLM — NOT a pip package. Install Ollama from https://ollama.com, then:
#     ollama pull qwen2.5:3b
# This is the default engine (JARVIS_LLM=local): first token ~0.45s when kept
# warm, versus 0.6-4.2s for the cloud, which is what blows the 1.5s budget.

# Native desktop UI window (renders ui/index.html as a standalone app, not a browser)
pywebview

# Local neural TTS option (offline, ~150ms/sentence, no network round-trip)
# Requires a voice model in piper_models/ (ru_RU-dmitri-medium.onnx + .onnx.json)
# from https://huggingface.co/rhasspy/piper-voices (ru/ru_RU/dmitri/medium).
piper-tts

# Cloud TTS. With TTS_ENGINE=edge it is the selected voice engine.
edge-tts

# Optional heavy voice cloning (XTTS). Use only if you want custom cloned voice.
# torch
# torchaudio
# soundfile
# TTS

# STT — Google (cloud fallback) + local faster-whisper (default, offline, GPU)
# WHISPER_MODEL=small by default: 'base' mis-hears the Russian wake word
# ("Жар весь" instead of "Джарвис") which makes Jarvis appear deaf.
speechrecognition
faster-whisper
# CUDA 12 runtime libs for GPU whisper on RTX 50-series (Blackwell).
# Required because CTranslate2 4.x is built against CUDA 12 (torch here is cu13).
# Set STT_ENGINE=google to disable local STT.
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12

# Audio playback
pygame

# Automation
pyautogui
pyperclip
pycaw
comtypes
screen-brightness-control
psutil
Pillow

# Web search
duckduckgo-search

# Google Calendar
google-auth
google-auth-oauthlib
google-api-python-client

# Personal Telegram account: chats, search, local export and confirmed sending.
Telethon>=1.36

# Note: ffmpeg is still recommended if you ever use XTTS.
# TTS_ENGINE is explicit: edge, piper, or xtts. Automatic selection is planned
# separately; the current committed example uses edge.
